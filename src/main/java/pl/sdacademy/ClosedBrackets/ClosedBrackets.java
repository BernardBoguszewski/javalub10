package pl.sdacademy.ClosedBrackets;

public class ClosedBrackets {

    public boolean checkIfClosed(String brackets) {         //zadanie nie dokonczone
        if (brackets.startsWith("[") && brackets.endsWith("]")) {
            return true;
        } else return false;
    }
}
